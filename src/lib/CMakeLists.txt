# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(cpu-bound-simulator STATIC 
  simulator.c)

include(Tornado.cmake)
set_tornado(cpu-bound-simulator)

target_include_directories(cpu-bound-simulator PUBLIC ../include)


target_link_libraries(cpu-bound-simulator PUBLIC 
  clog
  monotonic-time)

